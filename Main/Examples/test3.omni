/*/
    for (start_expr..end_expr) |var|
        expr;

    {
        let var: int = start_expr;
        while (var < end_expr){
            expr;
            var = var + 1;
        }
    }

    // for loop
    for (0..10) |i|{
        print("i: ");
        print(i);
        print("\n");
    }

    // for loop transformed into while loop
    {
        let i: int = 0;
        while (i < 10){
            {
                print("i: ");
                print(i);
                print("\n");
            }
            i = i + 1;
        }
    }
/*/

// let s1: str = "Hello\n";
// let s2: str = "World\n";
// 
// s2[0] = s1[0] = 'T';
// 
// print(s1);
// print(s2);
// 
// print(((s2 = "Ok")[0] = 'A'));
// print("\ns1: " + s1 + '\n');
// print("\ns2: " + s2 + '\n');

// let s: [3]str = {};
// s[0] = "Hello";
// s[1] = "World";
// s[2] = "Ok";
// 
// let int_cast: int = 0;
// print(((s[1] = s[0] = s[2] = "Test")[(int_cast = scan("input: "))] = 'A'));
// print('\n');
// 
// print("s[0]: " + s[0] + '\n');
// print("s[1]: " + s[1] + '\n');
// print("s[2]: " + s[2] + '\n');

// ------------------------------------------------------------------------------------------------

// let s1: str = "Hello";
// 
// s1[0] = 'A';
// 
// print("s1: " + s1 + '\n');

// let s: [3]str = {};
// s[0] = "Hello";
// s[1] = "World";
// s[2] = "Ok";
// 
// print(((s[0] = s[1][3] = s[2][0])[0] = "A") + '\n');
// 
// print("s[0]: " + s[0] + '\n');
// print("s[1]: " + s[1] + '\n');
// print("s[2]: " + s[2] + '\n');


let arr: [2]str = {};
arr[0] = "Hello";
arr[1] = "World";

for (0..array_size(arr)) |i|{
    let str_cast: str = i;
    print("arr[" + i + "]: " + arr[i] + '\n');
}

/*/
    // for loop
    for (0..10) |i|{
        print("i: ");
        print(i);
        print("\n");
    }

    print("------------------------------------------------\n");

    // for loop transformed into while loop
    {
        let i: int = 0;
        while (i < 10){
            {
                print("i: ");
                print(i);
                print("\n");
            }
            i = i + 1;
        }
    }
/*/

let s: str = "";

let int_cast: int = 0;
for ('\x20'..(int_cast = "128")) |i|{
    let char_cast: char = i;
    s = s + char_cast;
}

print(s + '\n');

print("\n---test success---\n");

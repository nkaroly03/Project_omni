/*/
    for (start_expr..end_expr) |var|
        expr;

    {
        let var: int = start_expr;
        while (var < end_expr){
            expr;
            var = var + 1;
        }
    }

    // for loop
    for (0..10) |i|{
        print("i: ");
        print(i);
        print("\n");
    }

    // for loop transformed into while loop
    {
        let i: int = 0;
        while (i < 10){
            {
                print("i: ");
                print(i);
                print("\n");
            }
            i = i + 1;
        }
    }
/*/

let s1: str = "Hello\n";
let s2: str = "Ok\n";
// 
s2[0] = s1[0] = 'T';
// 
// print(s1);
// print(s2);
// 
// print(((s2 = "Ok")[0] = 'A'));
// print("\ns1: " + s1 + '\n');
// print("\ns2: " + s2 + '\n');


// let s1: [2]str = {};
// s1[0] = "Hello";
// s1[1] = "Ok";
// 
// print(((s1[0] = s1[1] = "Test")[0] = 'A'));
// print('\n');
// 
// for (0..array_size(s1)) |i|
    // print(s1[i] + '\n');


// let arr: [2]int = {};
// 
// arr[0] = arr[(arr[1] = 1)] = 20;

// for (0..array_size(arr)) |i|{
    // let cast: str = "";
    // print("arr[" + (cast = i) + "]: " + (cast = arr[i]) + '\n');
// }

/*/
    // for loop
    
    for (0..10) |i|{
        print("i: ");
        print(i);
        print("\n");
    }

    print("------------------------------------------------\n");

    // for loop transformed into while loop
    {
        let i: int = 0;
        while (i < 10){
            {
                print("i: ");
                print(i);
                print("\n");
            }
            i = i + 1;
        }
    }
/*/

// let s: [1]str = {};
// 
// s[0] = "Hello, World!";
// // print("s[0]: " + s[0] + '\n');
// 
// s[0] = s[0] = "Hello";
// 
// print("s[0]: " + s[0] + '\n');

// print((argv[0] = "Hello"));
// 
// print(argv[0]);

// let s: str = "Hello";
// 
// print("s: " + s + '\n');
// s[0] = (s = "Test")[2];
// print("s: " + s + '\n');

print("Hello"[2 + 1]);

// print(
    // "\"hel\"lo"
    // ", "
    // "world"
    // "!"
    // "\n"
// );
// print(scan("val1: ") + scan("val2: "));
// print("\n");

let i: int = 32;
while ((i = i - 1) >= 0)
    print(~0xaaaaaaaa >> i & 1);
print("\n");
